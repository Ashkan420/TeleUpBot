# Buffer to store user messages
user_message_buffer = {}

# Track 'Done' messages sent to users so we can edit them all when posting
user_done_messages = {}

# Store last post link per user so remaining buttons can show it
user_last_post_link = {}
